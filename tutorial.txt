Lógica de programação
- variáveis
- operadores
- algorítmo
- estruturas de controle
- POO
- estruturas de dados

GIT e GITHUB

Comandos básicos do git:
git config --global user.name "Seu nome"
git config --global user.email "seuemail@email.com"
git clone url_do_repositório (clonar um repositório)
git add . (adiciona ao repositório local)
git status (verificar o status do repositório local)
git commit -m "tutorial" (documentar as alteraçõe)
git push (enviar ao repositório GitHub)
git pull (sincroniza as informações do repositório do GitHub) no pc
O aprendizado está em passos lentos.

Gerar token GitHub
1) Settings
2) Developer Settings
3) Token classic
4) Gerar o token selecionando tudo, guardar o token








